# У модели есть конфиги, а есть свойства
; https://docs.getdbt.com/reference/configs-and-properties

; --------------------------------------------------------- Конфиги ---------------------------------------------------
; Определяют каким образом будет осуществляться сборка модели данных (например тим материализации или действия которые происходят в бд до или после сборки модели, создание индекса)
; Могут указываться иерархически (конфиг всего проекта, конфиг папки, конфиг конкретной модели)




; ---------------------------------------------------------- Свойства -----------------------------------------------------
; Дополнительные атрибуты модели данных

; Свойства указываются в файлах с расширением .yml
; находится могут в любом месте папки models
; Свойства моделей: https://docs.getdbt.com/reference/model-properties