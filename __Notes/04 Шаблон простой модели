# Сначала идет конфиг с параметрами:

{{
    config(
        materialized = 'table',
    )
}}


# Затем запрос:

SELECT 
    *
FROM 

# Если ссылаемся на другую моджель, то используем ref

    {{ ref('stg_flights__bookings') }}