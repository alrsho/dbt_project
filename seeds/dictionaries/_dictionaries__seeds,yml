seeds:
    - name: city_region
      description: справочник регионов
      config:
        column_types:
            city: varchar(50)
            region: varchar(50)
            updated_at: timestamp
      columns:
        - name: city
          tests:                # Напоминаю - тесты не запускаются командой dbr seed, но запускаются командой dbt build или dbt tets
            - not null
            - unique
